---
- name: Download variables
  hosts: all
  become: yes
  vars_files:
    - secrets.yml

  tasks:
    - debug:
        msg: " {{ user1_password }}"
    - debug:
        msg: " {{ user2_password }}"

    - name: Start role
      include_role:
        name: user_role